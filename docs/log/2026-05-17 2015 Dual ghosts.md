# 2026-05-17 2015 - Dual ghosts

## What was done

- Split local personal-best ghost handling from Supabase top-board ghost handling.
- Added simultaneous blue top-board ghost and orange local PB ghost playback.
- Added a distance-scaled glow aura to ghost playback so far-away ghosts are easier to locate.
- Updated course/setup/results/records labels to distinguish personal bests from board bests.
- Ran `npm run build`.

## What worked

- Existing fixed-interval ghost samples were reusable for both replay lines.
- Supabase schema did not need to change because public runs already include full ghost payloads.
- Local personal bests can stay localStorage-backed while Supabase top-board runs remain public.

## What didn't and why

- First build failed because `ship.position` is a plain coordinate object, not a `THREE.Vector3`; the ghost glow distance check now uses a scratch `Vector3`.

## Decisions made

none

## Left unfinished

- Manual browser playtest still needs to confirm blue/orange visibility, glow strength, and behavior when PB and top-board ghost are the same run.

## state.md updated: yes
