# 2026-05-15 0008 - Agents spec orientation

## What was done

Updated `AGENTS.md` so future sessions read the active specs named in `docs/state.md` after reading state and decisions.

Clarified that active product/lore specs live at the root of `docs/spec/`, while historical or superseded specs live in `docs/spec/archive/`.

Added a warning not to treat `docs/spec/archive/` as current truth unless the user explicitly asks to revive, compare, or distill archived material.

Cleaned up the existing mojibake punctuation in `AGENTS.md` while preserving the same process rules.

## What worked

The process doc now matches the current two-spec setup without duplicating product direction inside `AGENTS.md`.

## What didn't and why

No code or gameplay implementation was changed.

## Decisions made

None.

## Left unfinished

None.

## state.md updated: yes
