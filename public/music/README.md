# Slingshot Music Drop Folder

Optional soundtrack files can be added here:

- `menu.mp3` - title, course board, pause, and settings
- `race.mp3` - active racing
- `results.mp3` - finish and invalid-run screens

The game works if these files are missing. It falls back to a quiet procedural drone and keeps all UI/gameplay sounds active.
